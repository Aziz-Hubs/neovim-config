return {
  "akinsho/toggleterm.nvim",
  version = "*",
  opts = {
    -- You can add custom configuration here if needed
    open_mapping = [[<c-\>]], -- Set a keymap to open the terminal
    direction = "float", -- Open as a floating window
  },
}
